GROUP #8

The table for 'registration' database was created using this SQL query:

CREATE TABLE `users` (
  `id` int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `username` varchar(100) NOT NULL,
  `email` varchar(100) NOT NULL,
  `password` varchar(100) NOT NULL
)

PHASE 1.1
 - Samoel Molhempour was responsible for front-end development of the web-site, as well as was testing the SQL database for vulnerabilities.
 - Bayron Pineda was responsible for back-end development of the web-site and little bit of front-end development.
 - Max Bogatyrov was working with the DB and was responsible for proper connection with the web-site and creating SQL injection prevention scripts.